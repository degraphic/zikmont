<form class="well" id="FrmNuevo" action="{{path('maestros_contabilidad_comprobantes_nuevo', { 'codigoComprobantePk':  0 })}}" method="post"> 
    <script>
        $('#myModal').modal('toggle');
        </script>

        <div class="modal hide" id="myModal">    
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">�</button>
                <h3>{%if arComprobante is not null%}Editar Comprobante{%else%}Crear Comprobante{%endif%}</h3>
            </div>    
            <div class="modal-body">                             
                <div class="well">      
                    <table>
                        {% if arComprobante is defined and arComprobante is not null %}
                        <tr>
                            <td><strong>C�digo:</strong><br/></td>
                            <td><input type="text" class="span4" readonly="true" name="TxtCodigoComprobante" {% if arComprobante is defined and arComprobante is not null %} value="{{arComprobante.codigoComprobanteContablePk}}" readonly="true" {% endif %}/></td>              
                        <tr> 
                        {% endif %}
                        <tr>
                            <td><strong>Descripci�n:</strong><br/></td>
                            <td><input type="text" class="span4" name="TxtNombreComprobante" {% if arComprobante is defined and arComprobante is not null %} value="{{arComprobante.nombreComprobanteContable}}" {% endif %} placeholder="Nombre" required autofocus/></td>              
                        <tr>                                        
                        </table>            
                    </div>
                </div>
            
            <div class="modal-footer">
                <a href="#" class="btn" data-dismiss="modal">Cancelar</a>                
                <button id="link_guardar" class="btn btn-primary" type="submit" name="OpSubmit" value="OpGuardar">Guardar</button>
            </div>  
        </div>
        </form>   
